<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.a8lambda8.carlog">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Analysis"
            android:label="@string/title_activity_analysis"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.a8lambda8.carlog.MainActivity" />
        </activity>
        <activity
            android:name=".FilterList"
            android:label="@string/title_activity_list"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.a8lambda8.carlog.MainActivity" />
        </activity>

        <receiver android:name=".CarConnectionReceiver">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
            </intent-filter>
        </receiver>
        <receiver android:name=".onBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!--<meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />-->

        <!--<service android:name=".CarLogAutoService" />-->

        <!--<receiver android:name=".CarLogAutoReceiver"
            android:permission=""
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="com.a8lambda8.carlog.ACTION_MESSAGE_READ" />
            </intent-filter>
        </receiver>
        <receiver android:name=".CarLogAutoReplyReceiver"
            android:permission=""
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="com.a8lambda8.carlog.ACTION_MESSAGE_REPLY" />
            </intent-filter>
        </receiver>-->
    </application>

</manifest>